package com.chickenfitness.model.enums;

public enum ExerciseType {
    STRENGTH,   // charge + reps
    BODYWEIGHT, // reps (charge optionnelle : lest)
    CARDIO,     // durée + distance
    TIMED       // durée (gainage, etc.)
}
